{
  "name": "updatedns",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "bun build ./index.ts --compile --minify --sourcemap --outfile out/updateDNS"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
